# Docker 与 Kubernetes 知识体系

## 目录

- [第一章 容器生态的演进与标准化](#第一章-容器生态的演进与标准化)
- [第二章 OCI 规范与容器运行时](#第二章-oci-规范与容器运行时)
- [第三章 容器运行的完整路径](#第三章-容器运行的完整路径)
- [第四章 Kubernetes 节点与工作负载](#第四章-kubernetes-节点与工作负载)
- [关键章节说明](#关键章节说明-标注)
- [推荐阅读顺序](#推荐阅读顺序)

---

## 第一章 容器生态的演进与标准化

> 从 Docker 的诞生到开放标准，理解容器世界如何从单一产品走向开放平台。

- 1.1 Docker 的起源——从 `docker run` 到标准集装箱
- **1.2 \* OCI 的诞生——容器世界的"宪法"（Runtime / Image / Distribution 三大规范）**
- **1.3 \* Docker 的拆分：containerd 的独立与捐赠**
- **[1.4 \* Docker、containerd 与 OCI 的完整调用链](docs/容器Docker、containerd%20与%20OCI.md)**
- 1.5 容器运行时生态地图：containerd、CRI-O、runc、crun、Kata、gVisor

---

## 第二章 OCI 规范与容器运行时

> 深入 OCI 三大规范的技术细节，掌握低级与高级运行时的分工边界。

- **2.1 \* OCI 运行时规范——config.json 的解剖（namespace、cgroup、seccomp、hooks）**
- 2.2 OCI 镜像规范——Manifest、镜像索引、文件系统层与内容寻址
- 2.3 OCI 分发规范——仓库 API 与镜像的推拉流程
- **[2.4 \* 运行时分层模型：低级运行时 vs 高级运行时](docs/OCI与运行时.md)**
- **2.5 \* 低级运行时详解：runc 的 create/start 流程与 clone 系统调用**
- 2.6 crun——C 语言实现的性能利器与 cgroup v2 原生支持
- 2.7 安全增强运行时：Kata Containers（硬件虚拟化）与 gVisor（用户态内核）
- **2.8 \* 高级运行时 containerd——插件架构、Snapshots、Task 与 Shim**
- 2.9 CRI-O——为 Kubernetes 而生的极简运行时

---

## 第三章 容器运行的完整路径

> 拆解 `docker run` 背后五层协作，理解每个组件的职责边界。

- **3.1 \* 分层模型总览：Daemon、高级运行时、Shim、OCI 包装器、低级运行时**
- 3.2 起点：Docker CLI 与 dockerd 的 Unix Socket 通信
- **3.3 \* containerd 的工作：镜像管理、rootfs 准备与 OCI 规格生成**
- **3.4 \* Shim 的作用——解除容器与 Daemon 的生命周期耦合**
- 3.5 runc 的执行：namespace、cgroup、rootfs 切换与安全配置
- **3.6 \* OCI 运行时包装器：nvidia-container-runtime（GPU 注入）与 kata-runtime（虚拟机沙箱）**
- **[3.7 \* 全景图：一条命令的完整执行流水线](docs/容器运行过程.md)**
- 3.8 Kubernetes 视角的变体：kubelet、CRI 与 pause 容器

---

## 第四章 Kubernetes 节点与工作负载

> 理解 K8s 的计算单元 Node 与调度抽象 Pod，掌握调度策略的核心机制。

- **4.1 \* Node 是什么——工作节点的组成（kubelet、容器运行时、资源容量）**
- **4.2 \* 为什么需要 Pod——共享网络命名空间、存储卷与调度原子性**
- 4.3 Pod vs Docker Compose：逻辑主机与独立容器的本质区别
- 4.4 Node 对象的组成：Conditions、Capacity 与 Allocatable
- **4.5 \* Labels（标签）——节点能力的语义化标记与动态管理**
- **[4.6 \* 调度策略：nodeSelector、Node Affinity 与 Taints & Tolerations](docs/K8s%20node%20和%20pod.md)**

---

## 关键章节说明（\* 标注）

| 章节 | 关键程度 | 理由 |
|------|----------|------|
| **1.2** OCI 规范 | ★★★ | 理解整个容器生态互操作性的前提；所有运行时均由此规范约束 |
| **1.3** Docker 拆分 | ★★ | 解释了为何 Kubernetes 弃用 dockershim，是架构演进的关键节点 |
| **1.4** 完整调用链 | ★★★ | 把 Docker、containerd、OCI 的关系串联起来的全局视角 |
| **2.1** config.json | ★★★ | OCI 运行时规范的核心，理解容器配置的精确语义 |
| **2.4** 运行时分层 | ★★★ | 低级/高级运行时的边界划分，是理解后续所有章节的基础 |
| **2.5** runc 流程 | ★★★ | 容器创建的本质：clone 系统调用与 namespace/cgroup 设置 |
| **2.8** containerd 架构 | ★★★ | 云原生标准运行时的设计哲学，Shim 机制是无缝升级的关键 |
| **3.1** 分层模型 | ★★★ | 整章的导览；明确各层职责才能理解后续每个组件的存在意义 |
| **3.4** Shim 机制 | ★★★ | 解释了 dockerd 重启时容器为何不中断，是工程实践的核心 |
| **3.6** OCI 包装器 | ★★ | 理解 `--runtime=nvidia` 究竟改变了哪一层，GPU 容器必读 |
| **3.7** 全景流水线 | ★★★ | 将所有概念串联为完整执行路径，是全章的总结与校验 |
| **4.2** Pod 设计 | ★★★ | Pod 是 K8s 最核心的抽象，理解它才能理解所有工作负载 |
| **4.5** Labels | ★★ | 调度策略的语义基础，没有标签调度器就是盲调 |
| **4.6** 调度策略 | ★★★ | nodeSelector、Affinity、Taints 是生产集群资源管理的三板斧 |

---

## 推荐阅读顺序

**入门路径**（建立容器全局观）：

> 1.4 → 3.7 → 4.2 → 4.6

**原理深究路径**（理解容器如何运行）：

> 1.2 → 1.3 → 2.1 → 2.4 → 2.5 → 2.8 → 3.1 → 3.4 → 3.6

**Kubernetes 路径**（专注 K8s 调度与节点管理）：

> 4.1 → 4.2 → 4.3 → 4.5 → 4.6 → 3.8
